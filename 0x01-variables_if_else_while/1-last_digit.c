#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*
 * main- Entry point of the program
 *
 * Return: zero when it does not work
 */
int main(void)
{
int n;

srand(time(0));
n = rand() - RAND_MAX / 2;

return (0);
}
